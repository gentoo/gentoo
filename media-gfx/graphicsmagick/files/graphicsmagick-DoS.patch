Patch taken from:
http://hg.code.sf.net/p/graphicsmagick/code/rev/f423ba88ca4e
To fix bug report:
https://bugs.gentoo.org/show_bug.cgi?id=626440
--- coders/png.c
+++ coders/png.c
@@ -6848,12 +6848,14 @@
                           png_error(ping, "Could not allocate trans_alpha");
 
                         for (i=0; i<(int) number_colors; i++)
+			  {
                           if (trans_alpha[i] == 256)
                              ping_trans_alpha[i]=255;
                           else
                              ping_trans_alpha[i]=(png_byte) trans_alpha[i];
                          (void) LogMagickEvent(CoderEvent, GetMagickModule(),
                             "    Alpha[%d]=%d",(int) i, (int) trans_alpha[i]);
+			  }
                       }
                   }
 
