--- a/contrib/cyrus-sasl-cmake/CMakeLists.txt
+++ b/contrib/cyrus-sasl-cmake/CMakeLists.txt
@@ -1,3 +1,9 @@
+find_package(PkgConfig REQUIRED)
+pkg_check_modules(SASL2 REQUIRED IMPORTED_TARGET libsasl2)
+add_library(_sasl2 INTERFACE)
+target_link_libraries(_sasl2 INTERFACE PkgConfig::SASL2)
+add_library(ch_contrib::sasl2 ALIAS _sasl2)
+return()
 if (${ENABLE_LIBRARIES} AND ${ENABLE_KRB5})
     set (DEFAULT_ENABLE_CYRUS_SASL 1)
 else()
