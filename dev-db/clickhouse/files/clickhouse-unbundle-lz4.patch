--- a/contrib/lz4-cmake/CMakeLists.txt
+++ b/contrib/lz4-cmake/CMakeLists.txt
@@ -1,3 +1,9 @@
+find_package(PkgConfig REQUIRED)
+pkg_check_modules(LZ4 REQUIRED IMPORTED_TARGET liblz4)
+add_library(_lz4 INTERFACE)
+target_link_libraries(_lz4 INTERFACE PkgConfig::LZ4)
+add_library(ch_contrib::lz4 ALIAS _lz4)
+return()
 # lz4 is the main compression method, cannot be disabled.
 
 set (LIBRARY_DIR "${ClickHouse_SOURCE_DIR}/contrib/lz4")
