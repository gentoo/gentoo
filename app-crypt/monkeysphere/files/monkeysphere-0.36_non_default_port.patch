--- a/src/share/common
+++ b/src/share/common
@@ -863,6 +863,10 @@
 		    ;;
 		('known_hosts')
 		    host=${userID#ssh://}
+	    	    if [[ "${host}" == *:* ]]; then
+			IFS=':' read -a ARR <<< "${host}"
+			host="[${ARR[0]}]:${ARR[1]}"
+		    fi
 		    remove_line "$keyFile" "$host" "$sshKey"
 		    ;;
 	    esac
