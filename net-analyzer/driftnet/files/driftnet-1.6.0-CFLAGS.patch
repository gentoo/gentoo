--- a/configure.ac
+++ b/configure.ac
@@ -79,12 +79,12 @@
     ,
     [enable_debug=yes])
 
-if test "x$enable_debug" = xyes; then
-    CFLAGS="-g -DDEBUG"
-else
-	CFLAGS="-O2 -DNDEBUG"
-fi
-#AM_CONDITIONAL(ENABLE_DEBUG, test "$enable_debug" = "yes")
+# if test "x$enable_debug" = xyes; then
+#    CFLAGS="-g -DDEBUG"
+# else
+#	CFLAGS="-O2 -DNDEBUG"
+# fi
+AM_CONDITIONAL(ENABLE_DEBUG, test "$enable_debug" = "yes")
 
 #
 # Checks for programs.
