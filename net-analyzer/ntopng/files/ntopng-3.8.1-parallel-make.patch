--- a/configure.seed
+++ b/configure.seed
@@ -526,11 +520,7 @@
 
 AC_DEFINE_UNQUOTED(_CRT_SECURE_NO_WARNINGS, 1, [Disable warning on windows])
 
-GMAKE=`which gmake`
-if test x$GMAKE = x
-then
-	GMAKE="make"
-fi
+GMAKE='$(MAKE)'
 
 GIT=`which git`
 if test x$GIT = x
