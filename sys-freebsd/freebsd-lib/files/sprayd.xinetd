service sprayd
{
        type = rpc
        socket_type = dgram
        protocol = udp
        wait = yes
        user = root
        server = /usr/libexec/rpc.sprayd
        rpc_version = 1
        disabled = yes
}
