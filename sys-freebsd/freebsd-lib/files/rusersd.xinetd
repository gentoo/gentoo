service rusersd
{
        type = rpc
        socket_type = dgram
        protocol = udp
        wait = yes
        user = root
        server = /usr/libexec/rpc.rusersd
        rpc_version = 1-2
        disabled = yes
}
