service rstatd
{
        type = rpc
        socket_type = dgram
        protocol = udp
        wait = yes
        user = root
        server = /usr/libexec/rpc.rstatd
        rpc_version = 1-3
        disabled = yes
}
