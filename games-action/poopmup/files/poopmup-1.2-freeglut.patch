--- poopmup.cpp
+++ poopmup.cpp
@@ -582,6 +582,7 @@
   getConfiguration();
   //Will overwrite the conf file params.
   ParseCommandLine(argc, argv); 
+  glutInit(&argc, argv);
   InitializeGL(argc, argv);	
   InitializeWorld();	
   InitMenu();
