/var/lib/mpd/log {
	missingok
	weekly
	delaycompress
	compress
	postrotate
		if type -p systemctl 2>&1 1>/dev/null && systemctl is-system-running 2>&1 1>/dev/null; then
			systemctl kill -s HUP mpd.service
		else
			/etc/init.d/mpd --quiet reload
		fi
	endscript
}
