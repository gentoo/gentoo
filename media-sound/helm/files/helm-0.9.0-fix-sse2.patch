diff --git a/Makefile b/Makefile
index 899ca9d..2cb4190 100644
--- a/Makefile
+++ b/Makefile
@@ -25,9 +25,11 @@ else
 ifneq (,$(findstring arm,$(MACHINE)))
 	SIMDFLAGS := -march=armv8-a -mtune=cortex-a53 -mfpu=neon-fp-armv8 -mfloat-abi=hard
 else
+ifneq (,$(findstring x86,$(MACHINE)))
 	SIMDFLAGS := -msse2
 endif
 endif
+endif
 
 
 PROGRAM = helm
