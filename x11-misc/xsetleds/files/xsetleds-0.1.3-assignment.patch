--- a/src/utils.c
+++ b/src/utils.c
@@ -38,7 +38,7 @@
 {
 	const char *p;
 
-	if (p = strrchr (filename, '/'))
+	if ((p = strrchr (filename, '/')))
 		p = strrchr (filename, '/') + 1;
 	else
 		p = filename;
