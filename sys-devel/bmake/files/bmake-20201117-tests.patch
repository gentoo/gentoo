--- a/unit-tests/Makefile
+++ b/unit-tests/Makefile
@@ -168,7 +168,6 @@ TESTS+=		dotwait
 TESTS+=		envfirst
 TESTS+=		error
 TESTS+=		# escape	# broken by reverting POSIX changes
-TESTS+=		export
 TESTS+=		export-all
 TESTS+=		export-env
 TESTS+=		export-variants
