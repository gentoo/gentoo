--- a/Makefile
+++ b/Makefile
@@ -56,6 +56,9 @@
 	CFLAGS += -I$(ZLIB_DIR)
 	LIBS = $(ZLIB_DIR)/libz.a
 endif
+ifeq ($(UNAME_S),Darwin)
+	LIBS += -liconv
+endif
 ifeq ($(UNAME_O),Cygwin)
 	CFLAGS += -DICONV_CHAR="const char"
 	LIBS += -liconv

