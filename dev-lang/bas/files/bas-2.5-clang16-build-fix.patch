Bug: https://bugs.gentoo.org/894774
--- a/getopt.c
+++ b/getopt.c
@@ -43,6 +43,7 @@ USA.  */
 #endif
 
 #include <stdio.h>
+#include <string.h>
 
 /* Comment out all this code if we are using the GNU C Library, and are not
    actually compiling the library itself.  This code is part of the GNU C
